'use strict';

angular.module('voiceUp')
    .controller('VerificationCtrl', ['$scope', 'semantria', 'comments',
        function ($scope, semantria, comments) {

            log.ctrl('Main');

        }]);
